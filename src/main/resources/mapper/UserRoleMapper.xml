<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.hz.dao.UserRoleMapper" >
  <resultMap id="BaseResultMap" type="com.hz.domain.UserRole" >
    <result column="user_id" property="userId" jdbcType="INTEGER" />
    <result column="role_id" property="roleId" jdbcType="INTEGER" />
    <result column="role_name" property="roleName" jdbcType="VARCHAR"/>
  </resultMap>
  <insert id="insert" parameterType="com.hz.domain.UserRole" >
    insert into shiro_user_role (user_id, role_id)
    values (#{userId,jdbcType=INTEGER}, #{roleId,jdbcType=INTEGER})
  </insert>
  <insert id="insertSelective" parameterType="com.hz.domain.UserRole" >
    insert into shiro_user_role
    <trim prefix="(" suffix=")" suffixOverrides="," >
      <if test="userId != null" >
        user_id,
      </if>
      <if test="roleId != null" >
        role_id,
      </if>
    </trim>
    <trim prefix="values (" suffix=")" suffixOverrides="," >
      <if test="userId != null" >
        #{userId,jdbcType=INTEGER},
      </if>
      <if test="roleId != null" >
        #{roleId,jdbcType=INTEGER},
      </if>
    </trim>
  </insert>

  <select id="userRoles" parameterType="java.lang.Integer" resultMap="BaseResultMap">
    select a.user_id,a.role_id,b.role_name from shiro_user_role a left outer join shiro_role b on a.role_id = b.id where user_id = #{userId,jdbcType=INTEGER}
  </select>
</mapper>